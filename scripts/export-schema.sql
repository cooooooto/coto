-- Script para exportar esquema desde producción
-- Ejecutar este comando en tu terminal:
-- supabase db dump --project-id YOUR_PROD_PROJECT_ID --schema public --file scripts/prod-schema.sql

-- Alternativamente, usar pg_dump directamente:
-- pg_dump "postgresql://postgres:[PASSWORD]@db.[PROJECT_ID].supabase.co:5432/postgres" \
--   --schema=public \
--   --schema-only \
--   --no-owner \
--   --no-privileges \
--   > scripts/prod-schema.sql
